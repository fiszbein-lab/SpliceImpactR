% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_ppi.R
\name{get_ppidm}
\alias{get_ppidm}
\title{Download and process the PPIDM dataset (Pfam-Pfam domain interactions)}
\usage{
get_ppidm(download = TRUE, load_dir = NULL, test = FALSE)
}
\arguments{
\item{download}{Logical; if TRUE, the PPIDM zip archive is
automatically fetched via \pkg{BiocFileCache}. If FALSE, assumes
the processed file already exists in \code{load_dir}.}

\item{load_dir}{Directory where files will be downloaded or read
from. The directory will be created if missing. If \code{NULL}, creates temp
dir.}

\item{test}{Logical; if TRUE, loads the minimal test set. If FALSE,
normal behavior}
}
\value{
A \code{data.table} containing Pfam-InterPro domain pairs.
}
\description{
Retrieves the 2021 PPIDM dataset from Zenodo, extracts only
\code{OutputData/} contents, merges them into a single table,
and maps Pfam accessions to InterPro IDs.
}
\examples{
ppidm <- get_ppidm(test=TRUE)
# ppidm <- get_ppidm(download = TRUE)

}
