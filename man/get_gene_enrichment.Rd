% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_multi_analysis.R
\name{get_gene_enrichment}
\alias{get_gene_enrichment}
\title{Unified gene selector for enrichment foregrounds}
\usage{
get_gene_enrichment(
  mode = c("di", "ppi", "domain"),
  x = NULL,
  res = NULL,
  hits = NULL,
  padj_threshold = 0.05,
  delta_psi_threshold = 0.1
)
}
\arguments{
\item{mode}{One of `"di"`, `"ppi"`, `"domain"`.}

\item{x}{Optional generic input. For `mode="di"`, this should be `res`-like;
for `mode="ppi"`/`"domain"`, this should be hits-final-like. If `x` is S4,
the appropriate slot is used automatically.}

\item{res}{Optional DI table (or S4) used when `mode="di"`. If provided, it
is preferred over `x`.}

\item{hits}{Optional hits-final-like table (or S4) used when
`mode="ppi"`/`"domain"`. If provided, it is preferred over `x`.}

\item{padj_threshold}{FDR cutoff used only for `mode="di"`.}

\item{delta_psi_threshold}{Absolute delta-psi cutoff used only for
`mode="di"`.}
}
\value{
Character vector of unique gene IDs.
}
\description{
Selects a foreground gene vector for enrichment from DI results (`res`) or
hits-final-like tables (`hits`) using a single wrapper.
}
\details{
- `mode = "di"` uses differential inclusion columns (`gene_id`, `padj`, `delta_psi`)
- `mode = "ppi"` uses hits-final-like columns (`gene_id`, `n_ppi`)
- `mode = "domain"` uses hits-final-like columns (`gene_id`, `diff_n`)

Inputs can be a `data.table`/`data.frame` or a `SpliceImpactResult` S4 object.
}
